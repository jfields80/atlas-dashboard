__pycache__/
*.pyc
.pytest_cache/

.venv/
venv/

.vscode/

*.db
atlas-database/

logs/
*.log

backups/

# Local demo website harness output (AES-WEB-002J.13)
generated-sites/

.env
.env.*

# Claude Code local files (AES-DEP-001)
.claude/

# Netlify local state (PETTRIPFINDER-PROD-005A). Never commit the CLI's local
# link state or any credential material. Assembled deploy bundles and their
# reports already live under the gitignored data/ tree (see below).
.netlify/
# WGE content-addressable store (AES-WEB-001 §9.1 layout; AES-WEB-002M.3
# pilot media ingestion writes here) — content-addressed local storage,
# never committed.
data/
