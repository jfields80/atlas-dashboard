# Atlas Investment OS — development / test dependencies (AES-DEP-001).
#
# Installs the full runtime set plus test tooling. Use this file to set up a
# development or CI environment:  python -m pip install -r requirements-dev.txt
# See docs/development/environment.md.

-r requirements.txt

# Test runner. The suite uses no pytest plugins (only the core runner).
pytest>=7.0,<10.0
