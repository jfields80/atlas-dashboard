"""Brand token taxonomy constants (AES-WEB-001 Phase 1 subset).

Full token taxonomies and default scales arrive with the Brand Engine
(Phase 2). Phase 1 pins only the taxonomy names the contracts reference.
"""

TOKEN_GROUP_PALETTE = "palette"
TOKEN_GROUP_TYPE_SCALE = "type_scale"
TOKEN_GROUP_SPACING_SCALE = "spacing_scale"

TOKEN_GROUPS = (
    TOKEN_GROUP_PALETTE,
    TOKEN_GROUP_TYPE_SCALE,
    TOKEN_GROUP_SPACING_SCALE,
)
