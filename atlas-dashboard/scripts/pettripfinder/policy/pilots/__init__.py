"""Bounded policy pilots.

A pilot is how a brand earns an adapter. Nothing in this package runs one; it
only defines the shape a run must take so that when an operator authorises a
pilot the result is a comparable artifact rather than an anecdote.
"""
