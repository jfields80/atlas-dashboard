{
  "schema": "ptf-discovery-attempt-ledger/1.0",
  "what_this_is": "Every paid discovery lookup this project has ever made -- a request to a places provider asking where a named property's official website is. Keyed on the PROPERTY and on the QUERY rather than on the identity key, so a rename, a re-census or a later work order cannot buy the same answer twice. Durable on purpose: the provider response cache expires, this does not.",
  "attempts": []
}